
**NAT (Network Address Translation)** - это технология, которая преобразует частные (локальные) IP-адреса в публичные и обратно. Позволяет нескольким устройствам использовать один публичный IP-адрес.

---

Преобразование адреса методом NAT может производиться почти любым маршрутизирующим устройством. Наиболее популярным является SNAT, суть механизма которого состоит в замене адреса источника при прохождении пакета в одну сторону и обратной замене адреса назначения в ответном пакете. Наряду с адресами источник/назначение могут также заменяться номера портов. "Порт (TCP/UDP)") источника и назначения.

Принимая пакет от локального компьютера, роутер смотрит на IP-адрес назначения. Если это локальный адрес, то пакет пересылается другому локальному компьютеру. Если нет, то пакет надо переслать наружу в интернет. Но ведь обратным адресом в пакете указан локальный адрес компьютера, который из интернета будет недоступен. Поэтому роутер «на лету» транслирует (подменяет) обратный IP-адрес пакета на свой внешний (видимый из интернета) IP-адрес и меняет номер порта (чтобы различать ответные пакеты, адресованные разным локальным компьютерам). Комбинацию, нужную для обратной подстановки, роутер сохраняет у себя во временной таблице. Через некоторое время после того, как клиент и сервер закончат обмениваться пакетами, роутер сотрёт у себя в таблице запись об n-м порте за сроком давности.

Помимо source NAT (предоставления пользователям локальной сети с внутренними адресами(Серый IP-адрес) доступа к сети Интернет часто применяется также destination NAT, когда обращения извне транслируются межсетевым экраном на компьютер пользователя в локальной сети, имеющий внутренний адрес (Серый IP-адрес) и потому недоступный извне сети непосредственно (без NAT).

---
# Виды NAT

Существует 3 базовых концепции трансляции адресов:
####  **[[Статический NAT (Static Network Address Translation)]]**

#### **[[Динамический NAT (Dynamic Network Address Translation)]]**

#### **[[Маскарадный NAT (NAT Overload, PAT)]]**

---

# Типы NAT

### **[[Симметричный NAT (Symmetric NAT)]]**

### **Cone NAT, Full Cone NAT**

### **Address-Restricted cone NAT, Restricted cone NAT**

### **Port-Restricted cone NAT**

---

























```c++
enum NATType {
	NO_NAT,              // Публичный IP
	FULL_CONE_NAT,       // Открытый для всех
	RESTRICTED_CONE_NAT, // Только к тем, кому отправлял
	PORT_RESTRICTED_NAT, // Только с тех портов, куда отправлял  
	SYMMETRIC_NAT,       // Разные порты для разных хостов
	SYMMETRIC_UDP_FIREWALL // Блокирует все входящие
};
```

## **Типы NAT (от простого к сложному)**

### 1. **[[Full Cone NAT (Открытый NAT)]]**
- **Принцип:** После исходящего соединения любой внешний хост может отправить пакет на внутренний узел
- **Аналогия:** Вы позвонили другу - теперь любой может вам позвонить
- **Поведение:**
  ```
  Внутренний: 192.168.1.10:54321 → Внешний: 95.165.133.44:12345
  Теперь ЛЮБОЙ внешний хост может отправить на 95.165.133.44:12345
  ```

### 2. **Restricted Cone NAT (Ограниченный NAT)**
- **Принцип:** Внешний хост может отправить пакет только если внутренний узел ранее отправлял ему пакет
- **Аналогия:** Вы позвонили другу - теперь только он может вам перезвонить
- **Поведение:**
  ```
  192.168.1.10 → 87.240.129.77 (любой порт)
  Теперь 87.240.129.77 может ответить на любой порт
  ```

### 3. **Port Restricted Cone NAT (Порт-ограниченный NAT)**
- **Принцип:** Внешний хост может ответить только с того порта, на который внутренний узел отправлял пакет
- **Аналогия:** Вы позвонили другу на конкретный номер - теперь он может перезвонить только с этого номера
- **Поведение:**
  ```
  192.168.1.10:54321 → 87.240.129.77:8080
  Теперь ответ возможен ТОЛЬКО с 87.240.129.77:8080
  ```

### 4. **Symmetric NAT (Симметричный NAT) - САМЫЙ СЛОЖНЫЙ**
- **Принцип:** Для каждого внешнего хоста создается УНИКАЛЬНОЕ преобразование портов
- **Аналогия:** Для каждого друга у вас отдельный телефонный номер
- **Поведение:**
  ```
  192.168.1.10:54321 → Хост A → NAT: 95.165.133.44:10001
  192.168.1.10:54321 → Хост B → NAT: 95.165.133.44:10002
  Разные внешние порты для разных хостов!
  ```

## **Практическое значение для P2P**

### **Простота установки соединения:**
- **Full Cone:** 🟢 Очень легко
- **Restricted Cone:** 🟢 Легко  
- **Port Restricted:** 🟡 Средняя сложность
- **Symmetric:** 🔴 Очень сложно (часто требует TURN-сервера)

### **Как это влияет на ваш проект:**

```cpp
class NATTraversal {
public:
    NATType detectNATType() {
        // Использует STUN-сервер для определения типа NAT
        // Возвращает один из типов выше
    }
    
    ConnectionStrategy getStrategy(NATType type) {
        switch(type) {
            case NATType::FullCone:
            case NATType::RestrictedCone:
                return ConnectionStrategy::UDP_HOLE_PUNCHING;
            case NATType::PortRestrictedCone:
                return ConnectionStrategy::UDP_HOLE_PUNCHING_WITH_TIMING;
            case NATType::Symmetric:
                return ConnectionStrategy::TURN_RELAY; // Через посредника
        }
    }
};
```

## **Определение типа NAT (STUN-протокол)**

Ваш клиент общается со STUN-сервером чтобы понять:

1. **Каков мой публичный IP:port?**
2. **Могут ли внешние хосты инициировать соединение?**
3. **Сохраняется ли порт для разных удаленных хостов?**

## **Статистика распространенности**

```
Port Restricted Cone NAT  ~65%  (самый распространенный)
Symmetric NAT            ~25%  (сложный случай)
Restricted Cone NAT      ~8%   (редкий)
Full Cone NAT            ~2%   (очень редкий)
```

## **Рекомендации для вашего P2P-проекта**

1. **Всегда начинать с UDP Hole Punching** - работает для 75% случаев
2. **Иметь fallback на TURN-сервер** - для симметричных NAT
3. **Использовать несколько STUN-серверов** для точного определения
4. **Поддержка TCP-фолбэка** если UDP недоступен

**Вывод:** Понимание типов NAT поможет вам выбрать правильную стратегию установления P2P-соединений и сделать ваш мессенджер работающим для большинства пользователей.