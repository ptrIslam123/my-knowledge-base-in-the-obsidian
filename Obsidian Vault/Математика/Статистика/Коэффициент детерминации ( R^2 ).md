Связанные темы: [[Дисперсия]]

**Коэффициент детерминации**, обозначаемый как **R²** (читается: "эр-квадрат"), — это **статистическая мера**, которая показывает, **насколько хорошо модель объясняет изменчивость зависимой переменной**.

> 🔍 Простыми словами:  
> **R² — это доля дисперсии (вариации) целевой переменной, которую удаётся объяснить моделью.**

---

## Формула коэффициента детерминации

$$
R^2 = 1 - \frac{SS_{\text{res}}}{SS_{\text{tot}}}
$$

Где:
- $$ SS_{\text{res}} $$ — **сумма квадратов остатков (residual sum of squares)**  
  $$
  SS_{\text{res}} = \sum_{i=1}^{n} (y_i - \hat{y}_i)^2
  $$
  Это сумма квадратов разностей между **реальными значениями** $$ y_i $$ и **предсказанными** $$ \hat{y}_i$$

- $$SS_{\text{tot}}$$ — **общая сумма квадратов (total sum of squares)**  
  $$
  SS_{\text{tot}} = \sum_{i=1}^{n} (y_i - \bar{y})^2
  $$
  Это сумма квадратов разностей между **реальными значениями** $$y_i$$ и **средним значением** $$ \bar{y}$$

---

## Интуитивное объяснение

Представьте, что вы пытаетесь предсказать **цены на дома**.

### Вариант 1: Нет модели
- У вас нет признаков, только данные.
- Лучшее, что вы можете сделать — **всегда предсказывать среднюю цену** $$ \bar{y} $$
- Ошибка такой "модели" — это $$ SS_{\text{tot}} $$

### Вариант 2: Есть регрессионная модель
- Вы строите модель (например, линейную регрессию) и получаете предсказания $$ \hat{y}_i $$
- Ошибка теперь — $$ SS_{\text{res}} $$

### Сравнение
- Если модель **абсолютно бесполезна**, то $$ SS_{\text{res}} \approx SS_{\text{tot}} → R^2 \approx 0 $$
- Если модель **идеально предсказывает**, то $$ SS_{\text{res}} = 0 → R^2 = 1 $$
- Если модель **хуже, чем просто среднее**, то \( R^2 < 0 \) (возможно при плохой модели).

---

## 📊 Интерпретация R²

| Значение R²    | Интерпретация                                                                                    |
| -------------- | ------------------------------------------------------------------------------------------------ |
| **R² = 1**     | Модель объясняет **100%** дисперсии. Идеальное предсказание.                                     |
| **R² = 0**     | Модель **не объясняет** никакой дисперсии. Не лучше, чем предсказание среднего.                  |
| **0 < R² < 1** | Модель объясняет часть дисперсии. Чем ближе к 1 — тем лучше.                                     |
| **R² < 0**     | Модель **хуже**, чем просто предсказание среднего. Сильно переобучена или неправильно построена. |

> 💡 Например, R² = 0.85 означает, что **85% изменчивости целевой переменной** объясняется моделью.

---

## Пример

Пусть у нас есть 5 домов:

| Реальная цена (y) | Предсказанная (ŷ) | Среднее (ȳ) |
|-------------------|-------------------|-------------|
| 100               | 105               | 110         |
| 120               | 118               | 110         |
| 110               | 110               | 110         |
| 130               | 125               | 110         |
| 90                | 92                | 110         |

### Шаг 1: \( SS_{\text{tot}} \)
$$
(100-110)^2 + (120-110)^2 + (110-110)^2 + (130-110)^2 + (90-110)^2 = 100 + 100 + 0 + 400 + 400 = 1000
$$

### Шаг 2: \( SS_{\text{res}} \)
$$
(100-105)^2 + (120-118)^2 + (110-110)^2 + (130-125)^2 + (90-92)^2 = 25 + 4 + 0 + 25 + 4 = 58
$$

### Шаг 3: \( R^2 \)
$$
R^2 = 1 - \frac{58}{1000} = 1 - 0.058 = 0.942
$$

> Модель объясняет **94.2%** дисперсии — очень хорошо!

---

## ⚠️ Важные свойства R²

| Свойство | Объяснение |
|--------|-----------|
| **Неотрицательный** | R² может быть отрицательным, если модель хуже среднего |
| **Масштабно-инвариантный** | Не зависит от единиц измерения |
| **Не показывает причинность** | Высокий R² не означает, что X вызывает Y |
| **Растёт с числом признаков** | Даже если признаки бесполезны (проблема переобучения) |
| **Не сравнивает модели с разными целевыми переменными** | Нельзя сравнивать R² для разных задач |

---

## 🔁 Связь с коэффициентом корреляции (для простой регрессии)

В случае **простой линейной регрессии** (один признак):

\[
R^2 = r^2
\]

где \( r \) — **коэффициент корреляции Пирсона** между \( x \) и \( y \).

> 🔹 Это означает, что R² — это **квадрат линейной связи** между переменными.

---

## Недостатки R² и как их преодолеть

### ❌ Проблема: R² всегда растёт с добавлением признаков

Даже если вы добавите **случайный шум** как признак, R² **немного увеличится**.

### ✅ Решение: **Скорректированный R² (Adjusted R²)**

$$
R^2_{\text{adj}} = 1 - \left( \frac{SS_{\text{res}} / (n - p - 1)}{SS_{\text{tot}} / (n - 1)} \right)
$$

где:
- \( n \) — число наблюдений,
- \( p \) — число признаков.

> Adjusted R² **штрафует** за лишние признаки и **может уменьшаться** при добавлении бесполезных переменных.

---

## 📈 Когда использовать R²?

| Сценарий | Подходит? |
|--------|----------|
| Сравнение моделей на одной выборке | ✅ Да |
| Оценка качества модели | ✅ Да (вместе с другими метриками) |
| Сравнение моделей с разным числом признаков | ❌ Лучше использовать Adjusted R² |
| Классификация | ❌ Нет, только для регрессии |
| Нелинейные модели | ✅ Да, если сравниваете объяснённую дисперсию |

---

## 🆚 R² vs Другие метрики

| Метрика | Плюсы | Минусы |
|--------|------|-------|
| **R²** | Интерпретируем, масштабно-инвариантен | Растёт с числом признаков |
| **MAE** | Прост в понимании (средняя ошибка) | Чувствителен к выбросам |
| **RMSE** | Учитывает большие ошибки сильнее | Тяжелее интерпретировать |
| **Adjusted R²** | Штрафует за лишние признаки | Сложнее интерпретировать |
