Сеть Хемминга — это **трёхслойная рекуррентная нейронная сеть**, предназначенная **для классификации бинарных векторов** на основе **расстояния Хемминга** — метрики, измеряющей количество позиций, в которых соответствующие элементы двух векторов различаются. Она была предложена **Ричардом Липпманном в 1987 году** как развитие идеи сети Хопфилда, но в отличие от неё, выдаёт не сам эталонный образ, а **номер класса**, к которому наиболее близок входной вектор.

---

## **Применения**

- **Распознавание символов** — например, рукописных цифр или букв.
- **Классификация изображений** — особенно бинарных (чёрно-белых).
- **Ассоциативная память** — восстановление эталонного образа по искажённому.

---

## **Особенности и преимущества**

- **Обучение за один проход** — веса задаются напрямую из эталонов, без итеративного обучения.
- **Устойчивость к шуму** — способна распознавать искажённые образы, если они сохраняют достаточное сходство с эталоном.
- **Конкурентная структура** — нейроны второго слоя конкурируют, что позволяет выбрать только один класс.

---

## **Архитектура сети Хемминга**

Сеть состоит из **трёх слоёв**:
1. **Входной слой** — принимает бинарный вектор (обычно с компонентами −1 и 1).
2. **Скрытый слой (слой Z)** — каждый нейрон вычисляет меру сходства между входным вектором и эталонным образом, используя **весовые коэффициенты**, полученные из эталонов.
3. **Выходной слой (слой A)** — рекуррентный слой с **обратными связями**, где нейроны конкурируют друг с другом, пока не останется только один активный нейрон — победитель, указывающий на класс.

---

### 📊 **Принцип работы**

#### 1. **Подготовка весов (обучение)**
- Веса первого слоя вычисляются по формуле:  
  \[
  w_{ij} = \frac{x_{ij}}{2}
  \]
  где \( x_{ij} \) — j-й компонент i-го эталонного образа.
- Веса второго слоя (рекуррентные) задаются матрицей:
  \[
  \begin{bmatrix}
  1 & -\epsilon & \dots & -\epsilon \\
  -\epsilon & 1 & \dots & -\epsilon \\
  \dots & \dots & \dots & \dots \\
  -\epsilon & -\epsilon & \dots & 1
  \end{bmatrix}
  \]
  где \( \epsilon \in (0, \frac{1}{K}] \), а K — количество классов.

#### 2. **Расчёт выходов**
- На вход подаётся вектор \( \vec{x^*} \).
- Первый слой вычисляет скалярные произведения между входом и эталонами.
- Второй слой работает **итерационно**, обновляя состояния нейронов до тех пор, пока **не останется только один активный нейрон** — он и указывает на победивший класс.

---

### ⚙️ **Функция активации**

Используется **пороговая функция**:
\[
f(s) = 
\begin{cases}
0, & s \leq 0 \\
s, & 0 < s \leq T \\
T, & s > T
\end{cases}
\]
где \( T = \frac{M}{2} \), а M — размерность входного вектора.

---

### 📌 **Вывод**

Сеть Хемминга — это **простая, но мощная модель**, особенно эффективная в задачах **классификации бинарных векторов**. Её ключевое преимущество — **высокая скорость обучения и устойчивость к искажениям**, что делает её полезной в системах распознавания образов и ассоциативной памяти.

---

Если тебе нужно, могу помочь с **примером реализации** на Python или **визуализацией работы сети** на конкретных данных.