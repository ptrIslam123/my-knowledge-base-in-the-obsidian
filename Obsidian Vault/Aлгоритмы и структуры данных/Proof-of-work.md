
### 1. **Определение**
**Proof-of-Work (PoW)** — это криптографический протокол, требующий от участника (например, майнера) выполнения вычислительно сложной, но легко проверяемой задачи в качестве доказательства того, что он потратил ресурсы (время, вычислительную мощность, энергию).

> Ключевой принцип:  
> **«Трудно найти, легко проверить»** — это основа PoW.


1.  **Цель ("загаданное число"):** Сеть задает **целевое значение**. Хэш блока должен быть **меньше** этого числа. На практике это действительно выражается в необходимости иметь `k` нулей в начале двоичной версии хеша.

2.  **Правила игры ("как угадывать"):**
    *   У всех майнеров на руках одни и те же **фиксированные данные** (транзакции, хэш предыдущего блока).
    *   Единственное, что они могут менять — это поле **`nonce`**.
    *   Они не могут "нарисовать" хэш. Они могут только подставлять в формулу разные `nonce` и смотреть, что получается.

3.  **Процесс ("перебор"):** Майнеры итеративно перебирают `nonce`: 0, 1, 2, 3... и для каждого вычисляют хэш. Это и есть та самая "угадайка" методом грубой силы.

4.  **Проверка ("проверка билета"):** Как только майнер находит такой `nonce`, что хэш становится валидным (меньше целевого числа), он объявляет об этом всей сети. Любой другой узел может мгновенно проверить победу, просто подставив этот `nonce` и данные блока в хэш-функцию и убедившись, что результат соответствует условию. Таким образом нельзя подделать хеш транзакции, так как любой участник может запросто тоже вычислить хеш с указанным `nonce` и удостовериться, действительно ли хеши совпадают.

---

### **Фундаментальный криптографический паттерн**

PoW реализует следующую структуру взаимодействия двух сторон — **доказывающего (Prover)** и **проверяющего (Verifier)**:

| Роль                        | Знания                                                                        | Действия                                                                                                       |
| --------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **Доказывающий** (майнер)   | Известен общий вход `data` (например, заголовок блока). **Не знает** `nonce`. | Подбирает `nonce` так, чтобы `H(data, nonce)` удовлетворял целевому условию (например, начинался с `k` нулей). |
| **Проверяющий** (сеть/нода) | Знает `data` и получает `nonce` от доказывающего.                             | Вычисляет `H(data, nonce)` и проверяет условие. Операция O(1), мгно                                            |

Это частный случай **интерактивного доказательства с нулевым разглашением (zk)** — доказывающий не раскрывает «секретный путь» поиска, только результат.

---

### **Механика PoW на примере Bitcoin**

#### Структура входных данных:
```text
H( version || prev_block_hash || merkle_root || timestamp || bits || nonce )
```
- `nonce` — 4-байтное поле (32 бита), которое майнер инкрементирует.
- Все остальные поля фиксированы для текущего блока (кроме, например, времени и транзакционного набора).

#### Условие PoW:
Найти такое `nonce`, чтобы:
```text
H(block_header) < target
```
Где `target` — это значение, определяемое **сложностью сети** (`difficulty`). Чем ниже `target`, тем труднее найти `nonce`.

💡 Например, `target` может быть `0x00000000FFFF000000000000...`, что требует ~16 нулей в начале хэша (в hex — ≈64 бит нулей).

#### Сложность и корректировка:
- Сложность (difficulty) пересчитывается каждые 2016 блоков (~2 недели в Bitcoin), чтобы поддерживать **среднее время нахождения блока ≈10 минут**.
- Формула коррекции:
  ```
  new_difficulty = old_difficulty × (2 weeks) / (actual_time_for_last_2016_blocks)
  ```

---

### **Зачем нужен PoW? Основные цели**

| Цель                                       | Объяснение                                                                                                                                         |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Синхронизация в распределённой системе** | PoW создаёт "временную задержку", позволяющую узлам прийти к консенсусу по порядку блоков.                                                         |
| **Защита от спама / Sybil-атак**           | Злоумышленник должен контролировать >50% хэшрейта, чтобы переписать историю. Это экономически нецелесообразно.                                     |
| **Справедливое распределение новых монет** | Награда за блок (в BTC — 6.25 BTC + комиссии) выплачивается майнеру, который первым нашёл `nonce`. Это вознаграждение за обеспечение безопасности. |
| **Невозможность предсказания победителя**  | Поиск `nonce` — рандомизированный процесс (brute force). Нельзя "забронировать" право на блок.                                                     |

---

### **Происхождение идеи: от Hashcash до Bitcoin**

| Год      | Событие                                             | Автор / Система  |
| -------- | --------------------------------------------------- | ---------------- |
| **1997** | Предложение **Hashcash** — PoW для борьбы со спамом | Adam Back        |
| **2004** | Использование Hashcash в BitGold (не реализован)    | Nick Szabo       |
| **2008** | Внедрение PoW в **Bitcoin** как ядро консенсуса     | Satoshi Nakamoto |

> Hashcash-идея:  
> Чтобы отправить email, клиент должен вложить 0.5 сек CPU-времени → для спамера рассылка 1 млн писем требует ~6 дней непрерывной работы одного ядра.

---

### 6. **Математическая модель PoW**

#### Вероятность нахождения `nonce` за одну попытку:
$$
P = \frac{1}{2^{n}}
$$
где `n` — количество ведущих нулевых бит в целевом хэше.

#### Ожидаемое число попыток (матожидание):
$$
E = 2^{n}
$$

#### Время поиска:
$$
T = \frac{2^{n}}{H}
$$
где `H` — хэшрейт (хэшей/сек).

**Пример (Bitcoin на ноябрь 2025)**:
- Целевой хэш ≈ 78 нулей в начале → `n ≈ 78`.
- `E ≈ 2^78 ≈ 3 × 10²³` попыток.
- Средний глобальный хэшрейт ≈ `600 EH/s` = `6 × 10²⁰` хэшей/сек.
- `T ≈ 500 с ≈ 8.3 мин` — близко к 10 мин (из-за распределения Пуассона и асинхронности сети).

---

### **Энергопотребление и критика**

| Проблема | Описание |
|---------|----------|
| **Высокое энергопотребление** | По Cambridge Bitcoin Electricity Consumption Index (CBECI), сеть BTC потребляет ~100–150 ТВт·ч/год — сопоставимо с малыми странами (Нидерланды, Финляндия). |
| **51%-атака** | Теоретически возможна при централизации хэшрейта (например, в мелких монетах). |
| **Углеродный след** | Зависит от энергомикса: в Китае — уголь, в США/Канаде — гидро/газ, в Исландии — геотермальная. |
| **Повторное использование ASIC** | ASICs для SHA-256 бесполезны вне майнинга → «цифровой мусор». |

> ✅ Контраргументы:  
> - PoW — единственный доказанно **безопасный и децентрализованный** механизм консенсуса на сегодня.  
> - Энергия «покупает» неизменяемость истории — это ценность.  
> - Майнеры часто используют **избыточную/флуктуирующую** (curtailed) энергию, которую иначе невозможно продать.

---

### **Альтернативы и гибриды**

| Метод | Описание | Отношение к PoW |
|-------|----------|-----------------|
| **Proof-of-Stake (PoS)** | Доказательство владения (например, ETH 2.0). Вместо работы — "ставка" (stake). | Замена PoW: меньше энергии, но другие риски (nothing-at-stake, неравенство владения). |
| **Proof-of-Space/Time** | Используется в Chia — доказательство выделенного места на диске. | PoW-подобный, но ресурс — storage, а не CPU. |
| **PoW + PoS (гибрид)** | Dash, Decred: PoW создаёт блоки, PoS — валидирует и управляет. | Сочетает безопасность PoW и управляемость PoS. |

---

### 9. **Интересные нюансы и оптимизации**

- **Nonce exhaustion**: В Bitcoin `nonce` — 4 байта → только 2³² значений. Если перебор не дал результата, майнеры меняют **extra nonce** в coinbase-транзакции → получают новый `merkle_root`, и цикл повторяется.
- **Stratum-протокол**: Пулы распределяют work units между майнерами, избегая дублирования.
- **ASIC-оптимизация**: Специализированные чипы достигают эффективности >100 GH/W, в то время как GPU — ~1 GH/W.
- **Selfish mining**: Атака, при которой майнер скрывает блоки, чтобы обогнать сеть. Требует ≥33% хэшрейта.

---

### **Генерация кошелька: приватный и публичный ключи**
Когда вы создаёте крипто-кошелёк (например, в Electrum, Ledger, Metamask — для BTC/EVM аналогично), происходит следующее:

1. Генерируется **случайное число** длиной 256 бит (32 байта) — это **приватный ключ** (`private key`, `sk`).  
   Пример (в hex):  
   ```
   e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
   ```

2. Из приватного ключа **детерминированно** вычисляется **публичный ключ** (`public key`, `pk`) с помощью эллиптической кривой **secp256k1**:  
   \[
   PK = sk \cdot G
   \]
   где \( G \) — генераторная точка кривой.

3. Из публичного ключа **односторонне** (хэшированием) получается **Bitcoin-адрес**:
   - `pk → SHA256 → RIPEMD160 → Base58Check` → адрес `1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa`  
   *(Это адрес Satoshi, кстати)*

> ✅ Приватный ключ → публичный → адрес: **однонаправлено**.  
> ❌ Обратно — **невозможно** (криптографическая стойкость ECDSA).

---

### **Как создаётся транзакция**

Допустим, вы хотите перевести 0.5 BTC **с вашего кошелька** на мой.

1. Вы формируете транзакцию:
   - **Inputs** (откуда берутся монеты): ссылка на UTXO, который вы хотите потратить.
   - **Outputs** (куда идёт): мой адрес + сумма; ваш адрес — сдача.
   - **ScriptSig** (подпись + ваш публичный ключ).

2. Вы **подписываете хэш всей транзакции** своим **приватным ключом**:
   $$
   \text{signature} = \text{ECDSA\_Sign}(sk, \, \text{hash(tx)})
   $$

3. В транзакцию вы добавляете:
   - вашу подпись (`signature`),
   - ваш публичный ключ (`pk`).

→ Транзакция отправляется в сеть.

---

### **Как идёт валидация(на стороне узлов и майнеров)**

Каждый узел при получении транзакции выполняет **криптографическую проверку**:

```text
ECDSA_Verify(
    pubkey = pk,                     // из транзакции
    message = hash(tx),              // хэш той же транзакции
    signature = sig                  // из транзакции
) → true или false
```

✅ Если подпись **действительно** создана с помощью `sk`, соответствующего `pk` → проверка проходит.  
❌ Если подпись подделана, или `pk` не соответствует `sk`, или хэш изменён — **проверка падает**.

> 💡 Узел **не спрашивает** «кто владелец?» — он **математически доказывает**, что владелец приватного ключа **одобрил именно эту транзакцию**.

---

### Почему вы **не можете подделать** транзакцию

Потому что:
- У биржи — свой приватный ключ (скрыт в их HSM/хранилище).
- Вы знаете только **их публичный ключ** (он входит в адрес, например, `3Fken...`).
- Чтобы подписать транзакцию *от их имени*, вам нужен **их приватный ключ**.
- **Без него**:
  - Вы можете *написать* транзакцию: «Перевести 100 BTC с адреса биржи на меня».
  - Но подпись, созданная *вашим* приватным ключом, **не пройдёт проверку** против *публичного ключа биржи*.
  - → Транзакция будет отвергнута при первой же валидации.

> Это как подделать подпись на чеке: даже если вы идеально нарисуете имя владельца счёта, банк проверит подпись по образцу — и откажет.

---

### ✅ Итог — три кита безопасности Bitcoin:

| Кит | Что обеспечивает |
|-----|------------------|
| **Криптография (ECDSA + SHA-256)** | Только владелец приватного ключа может подписать транзакцию. Подделка = невозможна (при текущем уровне науки). |
| **Модель UTXO + валидация узлами** | Каждая нода сама проверяет каждую транзакцию. Нет доверия — только математика. |
| **Proof-of-Work** | Делает перезапись *уже подтверждённой* истории экономически невыгодной. |

> **Приватный ключ = право распоряжаться средствами. Ничего больше не нужно — и ничто другое не поможет.**
